# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
venv/
.venv/

# Node / TypeScript
node_modules/
dist/
build/
*.tsbuildinfo

# AWS
.aws/
*.pem
*_accessKeys.csv
*accessKeys*

# Secrets — never commit
*.key
*secret*
*credential*
*private_key*

# OS
.DS_Store
Thumbs.db

# Test artifacts
tests/fixtures/
tests/tmp/

# Real corpus sample manifests -- contain real personal Dropbox file paths,
# never commit even though the folder itself is approved for migration.
scripts/route_audit/sample_*.json
scripts/route_audit/*.log
scripts/overhead_benchmark/files.json
scripts/overhead_benchmark/*.log

.vercel
